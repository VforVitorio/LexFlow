# LexFlow landing env
# This is the standalone marketing landing under landing/. The SPA's env is
# at ../frontend/.env.example — keep the two surfaces independent.

# Set by .github/workflows/deploy-landing.yml so the built bundle resolves
# under /LexFlow/ on GitHub Pages. Local dev keeps the default '/'.
# VITE_BASE_PATH=/LexFlow/
