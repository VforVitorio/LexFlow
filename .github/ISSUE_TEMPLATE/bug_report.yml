name: Bug Report
description: Reportar un error o comportamiento inesperado
title: "[Bug]: "
labels: ["bug", "triage"]
body:
  - type: markdown
    attributes:
      value: |
        Gracias por reportar este bug. Rellena la información necesaria para que podamos investigarlo.

  - type: textarea
    id: description
    attributes:
      label: Descripción del bug
      description: Describe de forma clara qué está fallando.
      placeholder: "Al intentar buscar una ley por nombre, la API devuelve un error 500..."
    validations:
      required: true

  - type: textarea
    id: expected
    attributes:
      label: Comportamiento esperado
      description: Qué esperabas que pasara.
      placeholder: "La API debería devolver una lista de leyes que coincidan con el término de búsqueda."
    validations:
      required: true

  - type: textarea
    id: actual
    attributes:
      label: Comportamiento actual
      description: Qué pasó realmente.
      placeholder: "La API devuelve un error 500 con traceback de KeyError."
    validations:
      required: true

  - type: textarea
    id: steps
    attributes:
      label: Pasos para reproducir
      description: Pasos mínimos para reproducir el error.
      placeholder: |
        1. Arrancar el servidor con `uv run python main.py`
        2. Hacer GET a `/laws?search=proteccion`
        3. Ver el error en la respuesta
    validations:
      required: true

  - type: dropdown
    id: component
    attributes:
      label: Componente afectado
      options:
        - API
        - Grafo
        - Chat
        - Dashboards
        - Frontend
        - Core / Parsers
        - CI/CD
        - Otro
    validations:
      required: true

  - type: input
    id: python-version
    attributes:
      label: Versión de Python
      placeholder: "3.12.x"

  - type: input
    id: os
    attributes:
      label: Sistema operativo
      placeholder: "Windows 11 / macOS 15 / Ubuntu 24.04"

  - type: textarea
    id: logs
    attributes:
      label: Logs o tracebacks relevantes
      description: Pega aquí cualquier output de error.
      render: shell

  - type: textarea
    id: context
    attributes:
      label: Contexto adicional
      description: Cualquier otra información que pueda ayudar.
